# 108MB of raw ERA5 pulls. Regenerate with scripts/allhours.mjs.
data/cache/
node_modules/
.DS_Store

# A local review table for data/resorts.json, regenerated on demand.
review.html

# Build artifacts of the social tooling. The generator and its render script
# are committed; what they emit is not, being reproducible from data/.
social/cards.html
social/review.html
social/out/
social/post-*/
social/picks/

# Snowfall estimates, regenerated by scripts/powder.mjs. Nothing refreshes
# these on a schedule the way forecast.json is refreshed, so a committed copy
# would sit stale; make.mjs runs without it.
data/powder.json
